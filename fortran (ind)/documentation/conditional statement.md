


```
IF (x > 0) THEN
	print *, "x is positive" 
END IF


IF (logical_expression) THEN 
	! Code to execute if the condition is true 
ELSE
	! Code to execute if the condition is false 
END IF

IF (logical_expression1) THEN
  ! Code to execute if logical_expression1 is true
ELSE IF (logical_expression2) THEN
  ! Code to execute if logical_expression2 is true
ELSE IF (logical_expression3) THEN
  ! Code to execute if logical_expression3 is true
! ... more ELSE IF blocks as needed
ELSE
  ! Code to execute if none of the above conditions are true
END IF
```